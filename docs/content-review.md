# Content review — 12 September 2026

## Historical articles

The Medium migration uses the author's complete public RSS item and the text
provided by the author. The supplied PDF was used to cross-check equations and
image dimensions. All 17 editorial images are local; tracking pixels and the
Medium advertising/recommendation interface are excluded. The video is a link,
not a third-party embed. The original animation attribution is preserved.
`writing-images.json` records original image URLs for both migrations.

The Blogspot migration preserves the full post body, four Mathematica derivation
pages, the diagram, animation and complete original Python 2/VPython code. Share
widgets and reader comments are not part of the migrated article. New technical
clarifications are labelled separately from the historical text.

## Research and projects

Publication authors were checked against the local PDFs. The IPMU paper's author
list and DOI were checked against the [University of Lisbon record](https://researchportal.ulisboa.pt/en/publications/information-retrieval-using-fuzzy-fingerprints/)
and the publisher DOI. The conference PDF host has an expired certificate, so
the site links to the university record and DOI instead of bundling an unverified download.

Public GitHub repository descriptions were checked for transformer-crypto
(Bitcoin price prediction), msc-dataset (ParlAI data cleaning), mcdc (C/Python
MC/DC), nemo-backend (Sword AI Challenge 2023), and receipt-sender (Google Sheets
to receipts). Forks are explicitly identified without inferring contributions.

Screenshots show public interfaces of Notícias do Dia, Workout Player (built-in
sample) and Split. Orbit Radar could not render WebGL in the capture environment;
its visual is explicitly an illustrative orbit diagram. xcos-mcp and PositNN also
use static explanatory diagrams. The autism card is an illustrative fictional
example, never a capture of the private application.
